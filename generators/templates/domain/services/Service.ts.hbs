import Service from "@/services/app/contracts/Service";
import {{pascalCase name}}Domain from "../types/{{pascalCase name}}Domain";
import {{pascalCase name}}Persistence from "../types/{{pascalCase name}}Persistence";
import {{pascalCase name}}Mapper from "../mappers/{{pascalCase name}}Mapper";

class {{pascalCase name}}sService
    implements
        Service<
            {{pascalCase name}}Domain,
            null,
            null
        >
{
    prefix = "/{{dashCase name}}/";

    async create({{lowerCase name}}: {{pascalCase name}}Domain): Promise<{{pascalCase name}}Domain> {
        //
    }

    async find(id: string | number): Promise<{{pascalCase name}}Domain> {
        //
    }

    async list(): Promise<{{pascalCase name}}Domain[]> {
        //
    }

    async update(
        id: string | number,
        {{lowerCase name}}: {{pascalCase name}}Domain
    ): Promise<{{pascalCase name}}Domain> {
        //
    }

    async delete(id: string | number): Promise<void> {
        //
    }
}

export default new {{pascalCase name}}sService();
